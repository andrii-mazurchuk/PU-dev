The pu unit has no GitHub mirror yet. Build something that reads open issues from a repo and creates one matching task per issue, one-way only - GitHub stays the source of truth for the issue body, so the task should just carry the issue URL. It must be re-runnable without creating duplicates. Once that works I want to look over the first mirrored batch myself before we let anything run against it.